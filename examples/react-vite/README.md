# VibeTip — React + Vite

ESM `init()` API를 React에서 쓰는 패턴입니다: `useEffect`에서 생성하고 cleanup에서 `destroy()`, 반환된 핸들로 `open()`/`close()` 제어.

```bash
bun install
bun run dev
```

npm/pnpm/yarn로도 동일하게 동작합니다 (`npm install && npm run dev`).
